---
- block: 
  - name: Install and configure QuestDB
    ansible.builtin.include_tasks: "install.yml"

  - name: Setup database
    ansible.builtin.include_tasks: "setup_database.yml"
  
  when: create | default(true)