---
- name: Create InfluxDB directories
  ansible.builtin.file:
    path: "{{ item }}"
    state: directory
    mode: "0755"
  loop:
    - /root/.influxdb
    - /root/.influxdb/data

- name: Create symbolic link
  ansible.builtin.file:
    src: /root/.influxdb/
    dest: /influxdb
    state: link